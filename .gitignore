# ============================================================
# WHITELIST MODE — só o marketplace e os três plugins Wire (base, secops, devkit).
# Tudo o resto fica IGNORADO por defeito.
# ============================================================

# 1. Ignora absolutamente tudo
/*

# 2. Whitelist apenas estes paths
!.gitignore
!README.md
!LICENSE
!.claude-plugin/
!base/
!secops/
!devkit/
!scripts/
!.github/

# 3. Detritos ignorados em qualquer subpasta
**/.DS_Store
**/__MACOSX
**/*.log
**/*.bak
**/*~
**/*.plugin
secops/.orphaned_at
